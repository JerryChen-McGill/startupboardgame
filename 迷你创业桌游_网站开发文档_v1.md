# 迷你创业桌游网站开发文档

> 文档版本：v2.0  
> 更新日期：2026-07-29  
> 当前网站：https://jerrychen-mcgill.github.io/startupboardgame/  
> 源码仓库：https://github.com/JerryChen-McGill/startupboardgame  
> 用途：记录网站当前已经实现的产品规则、数据约束、视觉规范和继续开发方式。

---

## 1. 当前产品定义

### 1.1 一句话介绍

> 强强合作，把离谱点子，拼成自洽生意。

“迷你创业桌游”是一款适合 4–6 人的创业合作与竞争桌游。玩家通过明确分工、讨论卡牌关系、应对外部变化，把用户、传播、需求、产品四张卡组成 2×2 创业项目。

### 1.2 为什么做这款游戏

游戏主要挑战两种合作能力：

1. **分工明确性**：每个创业元素都有清楚的负责人；
2. **应变敏捷性**：外部变化出现时，团队能够迅速交换方案、重新讨论和调整。

### 1.3 四类卡牌与固定位置

| 位置 | 卡类 | 英文标签 | 类别背景色 | 核心问题 |
| --- | --- | --- | --- | --- |
| 左上 | 用户 | USER | `#73B97C` | 为谁？ |
| 右上 | 传播 | SPREAD | `#AC85D7` | 怎么让他们知道？ |
| 左下 | 需求 | NEED | `#F0A15A` | 怎么了？ |
| 右下 | 产品 | PRODUCT | `#67A5D1` | 怎么办？ |

每张卡比例为 3:4。四张卡按 2×2 拼合后，整体仍为 3:4 大长方形。

---

## 2. 已确认的关系模型

### 2.1 只允许四种相邻关系

数据模型和游戏判定只允许：

1. 用户—传播；
2. 用户—需求；
3. 传播—产品；
4. 需求—产品。

以下两个对角组合不需要建立关系，也不得出现在 `relationMeta` 中：

- 用户—产品；
- 需求—传播。

### 2.2 当前关系数据

`data/game-data.json` 中共有 72 条示例关系，每类 18 条：

| 关系类型键 | 数量 |
| --- | ---: |
| `user-promotion` | 18 |
| `user-need` | 18 |
| `product-promotion` | 18 |
| `need-product` | 18 |

关系数据是设计示例，不是唯一标准答案。玩家仍需解释为什么关系合理。

### 2.3 卡牌边缘映射

卡牌按 2×2 固定位置显示，因此每类卡只需要两条关系边：

| 卡类 | 多色接收边 | 单色输出边 |
| --- | --- | --- |
| 用户 | 右边接收可匹配的传播色 | 下边延伸用户自己的专属色 |
| 传播 | 下边接收可匹配的产品色 | 左边延伸传播自己的专属色 |
| 需求 | 上边接收可匹配的用户色 | 右边延伸需求自己的专属色 |
| 产品 | 左边接收可匹配的需求色 | 上边延伸产品自己的专属色 |

边缘使用粗色条。每张卡的输出边始终只有本卡的一种专属色；接收边可以包含多个并列色段，每个色段使用“可匹配的另一张卡”的专属色。四类卡由此形成“传播 → 用户 → 需求 → 产品 → 传播”的颜色闭环。

---

## 3. 玩家人数、回合与融资

### 3.1 人数

- 适合 4–6 人；
- 每回合固定 4 人组成创业团队；
- 多出的玩家成为外部竞争对手；
- 以 6 人局为例：4 人合作创业，2 人作为竞争对手。

### 3.2 当前回合流程

1. 分配本回合创业团队与竞争对手；
2. 创业团队分别处理用户、传播、需求、产品；
3. 从四类卡中各选择一张，组成 2×2 项目；
4. 讨论并解释四条相邻关系；
5. 根据关系数量结算融资；
6. 下一回合轮换角色；
7. 多回合结束后，累计融资最多的玩家获胜。

### 3.3 事件卡状态

竞争对手可以通过事件卡干扰创业进程，但事件卡仍是后续开发内容。当前网站只介绍这一方向，不生成尚未确认的事件卡规则或数据。

### 3.4 融资公式

| 成立的相邻关系 | 项目状态 | 创业团队每人融资 |
| ---: | --- | ---: |
| 4 | 四处全部连通 | 200 万 |
| 3 | T 字形连通 | 100 万 |
| 0–2 | 至少一张卡孤立，创业失败 | 0 |

用户—产品与需求—传播不参与关系数量统计。

---

## 4. 卡牌视觉系统

### 4.1 类别背景

四类卡牌分别使用浅绿、浅紫、浅橙、浅蓝背景。背景色表示卡类，不能被专属色替代。卡面使用同类色细内框，保持参考图中的纸质桌游感和信息层级。

### 4.2 专属色光环

每类的 10 张卡分别使用一套不重复的专属色：

| 键 | 中文 | 色值 |
| --- | --- | --- |
| `red` | 红 | `#E5524A` |
| `orange` | 橙 | `#F08A3E` |
| `yellow` | 黄 | `#F4C542` |
| `green` | 绿 | `#43A66B` |
| `cyan` | 青 | `#31B7B3` |
| `blue` | 蓝 | `#4D8FE5` |
| `purple` | 紫 | `#8E67D5` |
| `white` | 白 | `#F8F5EC` |
| `black` | 黑 | `#25232D` |
| `brown` | 棕 | `#8A5B3D` |

专属色根据卡牌图像的主要颜色做大体匹配，存放在每张卡的：

- `accentKey`
- `accentName`
- `accent`

图像外围显示圆形发光光环。白色和黑色必须保留描边或阴影，确保在类别背景上可见。

### 4.3 关系色条

- CARD LIBRARY 展示该卡在完整 `RELATIONSHIP MAP` 中的所有可匹配对象；
- STARTUP LAB 只展示本次选中四张卡之间实际成立的关系；
- 输出边使用本卡的一条完整专属色，接收边才按匹配对象拆成多色段；
- 色条宽度应足够明显，当前网页实现约 9–10 px；
- 无匹配关系的边保持空白；
- 关系边条不使用两个对角关系。

---

## 5. 网站信息架构

当前单页顺序：

1. Hero：一句话介绍；
2. Why：为什么做这款游戏；
3. Rulebook：人数、角色、回合流程与融资说明；
4. CARD LIBRARY：40 张卡、类别背景、专属光环和完整关系边条；
5. PHYSICAL PROTOTYPE：保留单卡原型 A 与盒内物料；
6. RELATIONSHIP MAP：40 张卡、72 条初始关系与可编辑确认工作台；
7. STARTUP LAB：随机抽四张、显示实际边条并结算融资；
8. 页脚。

已删除：

- “游戏内核”区块；
- 原型 B 四卡静态面板；
- DESIGN PRINCIPLES 区块；
- “WHO · WHAT · HOW · TELL”结尾区块。

---

## 6. 实现说明

### 6.1 技术结构

网站为无依赖静态站点：

- `index.html`：页面结构与静态文案；
- `styles.css`：响应式布局、卡牌视觉与关系边条；
- `app.js`：卡牌库、可编辑关系图、本地草稿、修改记录、JSON 导出、STARTUP LAB 与融资判定；
- `data/game-data.json`：卡牌、专属色、关系和元数据；
- `scripts/sync-approved-data.mjs`：同步确认的数据与色彩；
- `scripts/generate-network.mjs`：生成独立 SVG 关系图；
- `tests/game-data.test.mjs`：数据约束测试。
- `tests/relationship-editor.test.mjs`：关系工作台结构、确认状态与导出能力测试。

### 6.2 关键前端常量

`app.js` 中：

- `STARTUP_CARD_ORDER`：`user, promotion, need, product`；
- `STARTUP_PAIR_TYPES`：仅四种相邻配对；
- `CARD_EDGE_CONFIG`：四类卡牌的两条有效边。

任何后续实现都应复用这些约束，不得重新加入两个对角关系。

### 6.3 RELATIONSHIP MAP 编辑器

- 四类共 40 张卡全部进入关系图，沿矩形四边排列；
- 卡牌和关系都包含 `confirmed` 布尔状态，旧数据缺少该字段时按 `false` 处理；
- 未确认卡牌块和关系线显示为虚线，确认后显示为实线；
- 卡牌支持新增、修改、删除、确认与取消确认；
- 关系支持新增、修改、删除、确认与取消确认；
- 关系新增或修改时只允许四种相邻类别组合；
- 删除卡牌时同步删除所有相关关系并逐条写入日志；
- 编辑草稿与操作日志保存在浏览器 `localStorage`；
- “导出 JSON”只导出修改后的完整游戏数据；
- “导出修改记录”仅列出被修改的卡牌或关系位置，以及对应字段修改成了什么。

### 6.4 STARTUP LAB 判定

STARTUP LAB：

1. 从四类核心卡各抽一张；
2. 只创建四个相邻 pair；
3. 检索关系数据；
4. 统计成立关系数；
5. 根据 4 / 3 / 0–2 的公式显示融资；
6. 在相邻边分别显示单色输出与实际成立关系的接收色。

### 6.5 本地运行与检查

```powershell
python -m http.server 4173
node --check app.js
node --test
node scripts/sync-approved-data.mjs
node scripts/generate-network.mjs
```

### 6.6 部署

推送到 `main` 后，`.github/workflows/deploy-pages.yml` 自动发布 GitHub Pages。

---

## 7. 后续开发边界

可继续开发：

- 事件卡与竞争对手干扰机制；
- 回合轮换与累计融资记录；
- 4 人、5 人、6 人的正式角色轮换表；
- 完整纸质说明书；
- 卡牌印刷尺寸、出血和生产文件；
- 更多经测试的关系数据。

在未确认前不要自行确定：

- 一局总回合数；
- 事件卡数量、强度和使用时机；
- 平局处理；
- 竞争对手是否获得直接融资；
- 玩家能否创造关系图中不存在的新关系；
- 卡牌交换次数与讨论时限。

这些内容必须继续标记为“待测试/待确认”。
